# .cargo/config.toml — SGIF Monorepo
# Forces cargo to use the rustup-managed toolchain, bypassing the Nix system rustc
# that causes --diagnostic-width incompatibility.

[build]
rustc = "/home/rocha/.rustup/toolchains/stable-x86_64-unknown-linux-gnu/bin/rustc"
